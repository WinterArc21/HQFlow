/** Backward-compatible type-only exports from the browser-safe wire contract. */
export type { CodeHQSnapshot, CodeHQStatus, RepositoryInfo, RepositoryMapRecord, WorkflowRecord } from "@schema/wire";
